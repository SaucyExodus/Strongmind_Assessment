<% content_for :title, "New Pizza" %>

<div class="card p-4 shadow-sm">
  <h1 class="mb-3">Add a New Pizza</h1>

  <%= render "form", pizza: @pizza %>

  <div class="mt-3">
    <%= link_to "Back", pizzas_path, class: "btn btn-secondary" %>
  </div>
</div>